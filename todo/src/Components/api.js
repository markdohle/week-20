//===========API Simulation for Jest Test===============
export const api = {
    createItem: (newItem) => {
      return Promise.resolve(newItem);
    }
  }
  //======================================================